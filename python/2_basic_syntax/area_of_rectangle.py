# Create a function to find and show the area of a rectangle with known dimensions
def area(length, width):
    calculated_area = float(length) * float(width)
    print("Rectangle area: ", calculated_area)


area(10, 20)
