#THE POLITE AREA CALCULATOR 1.0

width = input("What is the width of the gosh dang rectangle? (use numbers only, pls) ")
length = input("And the length? (you can use floating point, I don't care) ")

def area():
	print ("The area is", float(length)*float(width),", your'e welcome")
area()